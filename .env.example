# Gemini API Key (required for text generation)
API_KEY=your-gemini-api-key

# Local Flux API Server (for image generation)
FLUX_API_URL=http://localhost:8000
FLUX_API_KEY=your-flux-api-key

# Backend API URL (optional - falls back to local IndexedDB if not available)
VITE_API_URL=http://localhost:3001/api

# Alternative: fal.ai API (if not using local Flux server)
# FAL_API_KEY=your-fal-api-key
